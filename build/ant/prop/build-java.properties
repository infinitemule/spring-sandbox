#
# Java Build Properties
#
work.dir=./work/java
work.src.dir=${work.dir}/src
work.classes.dir=${work.dir}/classes
work.lib.dir=${work.dir}/lib

src.dir=./src/java
conf.dir=./conf
lib.dir=./lib

dist.org=${proj.org}
dist.dir=${build.dir}/dist
dist.artifact=${proj.name}

dist.version.major=0
dist.version.minor=1

dist.version=${dist.version.major}.${dist.version.minor}
